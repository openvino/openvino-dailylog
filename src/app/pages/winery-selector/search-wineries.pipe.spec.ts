import { SearchWineriesPipe } from './search-wineries.pipe';

describe('SearchWineriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWineriesPipe();
    expect(pipe).toBeTruthy();
  });
});
